package sortcompare.structures;

import java.util.PriorityQueue;

/**
 * Priority heap. Currently just an extension of PriorityQueue but will
 * eventually be replaced with a custom implementation.
 * @author Oliver
 * @param <E> The type of Object the heap will contain.
 */
public class CustomHeap<E> extends PriorityQueue<E> {

}
